# 招商企业服务与智慧监管平台系统需求分析说明书

> **文档版本：V5.0深化版（与 demo 高保真原型严格对齐）**
> **文档定位：以用户实际工作过程为主线、产品设计与开发可执行、项目验收可验证的统一需求基线**
> **编制依据：V3.1、V4.0、V5.0 三版需求说明书，及其已落地的业务原型（demo 项目）**
> **适用对象：项目业主、业务部门、产品经理、UI/UX 设计师、前端开发、测试人员、实施与验收人员**

---

# 一、文档说明

## 1.1 编制目的

本说明书将"招商企业服务与智慧监管平台"从业务规划与高保真原型，进一步固化为一份可直接指导产品设计、前端开发、数据建设和项目验收的统一需求基线。

本版本以**用户实际工作过程**为主线，按照：

> **用户目标 → 业务场景 → 功能需求 → 页面与交互 → 数据需求 → 技术实现 → 验收标准**

组织需求，并**严格以 `demo` 高保真原型的真实实现为唯一功能基准**——文档中描述的每一处系统架构、功能菜单与功能点，均与 `demo` 项目当前代码一致；凡 `demo` 尚未实现的部分（后端服务、真实鉴权与多角色权限、真实 AI 模型调用、企业侧服务门户等），本文一律明确标注为"规划能力 / 不在本次原型范围"，不得写成已实现功能。

## 1.2 与历史版本的继承关系

- **V3.1**：确立平台总体定位、企业全生命周期管理、企业画像、风险监管、招商项目、政策服务、AI 与业务融合、数据治理、权限安全、技术架构等顶层框架。核心原则是"**业务系统是主体，AI 是智能化运行能力，智能体嵌入业务流程而非独立割裂平台**"。
- **V4.0**：细化招商驾驶舱、企业档案、企业 360° 画像、风险预警、招商项目六阶段、政策服务、产业关系、AI 综合研判、四类核心智能体、全局搜索、页面下钻、风险/项目/政策三条业务闭环等交互。
- **V5.0**：在 V4.0 原型基础上，将功能清单**收敛并严格对齐 demo 真实实现**，使需求说明书成为 demo 的可验证镜像。

## 1.3 阅读与使用约定

- 功能编号沿用 `FR-<模块>-<序号>` 格式，便于需求追踪。
- 文中"原型已实现 / 原型未实现"以 `demo/` 目录代码为判据。
- 数据规模、菜单项、页面功能点均取自 `demo/assets/data/mock.js` 与 `demo/assets/js/pages/*.js` 的真实实现。

---

# 二、产品总体定位

## 2.1 产品定位

> **以招商企业服务和监管为核心、以数据为基础、以业务流程为主线、以 AI 为智能化能力的招商企业服务与智慧监管平台。**

平台不是"招商业务系统 + 一个独立 AI 聊天平台"，而是将企业、项目、经营、风险、政策、产业和决策统一到一个业务工作空间中，并让 AI 成为业务系统的智能化运行能力。

## 2.2 总体产品结构

平台由 **4 个导航分组、9 个功能模块** 构成：

1. 招商总览：招商驾驶舱
2. 企业监管：企业概况、企业画像、风险预警、关系图谱
3. 招商引资：我的工作台、招商项目、政策服务
4. 智能应用：招商智能体

> 说明：企业服务（诉求/走访）以**企业画像内的"企业服务"按钮**、政策推送以**政策服务页的"⟳ 同步政策" + "✦ AI 智能匹配"回写企业画像**等形式嵌入业务流程，不再单列独立菜单项。

## 2.3 核心业务逻辑

```text
企业进入平台（内置演示数据集）
      ↓
企业信息归集 → 形成统一企业档案
      ↓
形成企业 360° 画像（概况 / 经营状态 / 经营趋势 / 关系网络 / 企业风险 / AI 综合研判）
      ↓
持续监测经营、项目、风险、政策等变化
      ↓
发现异常和机会 → AI 分析与研判
      ↓
形成风险事件 / 项目建议 / 服务事项 / 任务 / 报告
      ↓
进入业务流程（派发处置 / 项目推进 / 政策兑现 / 任务办理）
      ↓
责任人员处理 → 形成处理结果
      ↓
领导决策与招商管理（驾驶舱总览）
```

---

# 三、用户角色与登录

## 3.1 角色总览

demo 原型为**单角色演示形态**：提供统一的"系统管理员"工作台，演示账号 `admin / admin123`，登录后顶栏展示账号名 `admin` 与角色名"系统管理员"。原型**未实现**后端真实鉴权与多角色权限隔离（RBAC 为规划能力，详见第三十三章）。

| 角色（规划） | 核心工作台（demo 中的对应入口） | 备注 |
| --- | --- | --- |
| 主管领导 | 招商驾驶舱 | 驾驶舱即领导总览 |
| 招商专员 / 部门人员 | 我的工作台、企业概况、企业画像、风险预警、招商项目、政策服务 | demo 不区分角色，统一以"系统管理员"身份进入 |
| 风险管理人员 | 风险预警 | 同上 |
| 产业 / 招商谋划人员 | 关系图谱、招商智能体 | 同上 |

## 3.2 用户登录

- **登录页**：复刻前后端分离项目的登录页与背景插画（星河·数据网络·波澜 SVG），含用户名/密码输入、登录按钮、错误提示、演示账号提示。
- **凭据校验**：演示账号 `admin / admin123`；账号或密码错误时提示"用户名或密码错误"。
- **登录态持久化**：以 `localStorage` 键 `zs_token` 保存，刷新不丢失；未登录时由 `#loginScreen` 覆盖主应用，避免主界面闪现。
- **注销**：点击顶栏用户区可退出登录（二次确认），回到登录页。
- **规划能力（原型未实现）**：真实 JWT 鉴权、多角色权限矩阵、操作审计日志。前后端分离版（`server/` + `web/`）已具备 JWT 登录与多角色接口，可作为后续落地参考。

---

# 四、系统总体架构

## 4.1 架构定位

`demo` 是一个**纯前端、零后端依赖的高保真交互原型**。所有数据与业务能力均在前端运行时完成，可直接以浏览器打开 `demo/index.html` 离线运行，亦可经静态服务器托管发布。本文档的"系统架构"即描述该原型的真实分层与技术构成；后端服务（数据接入、真实鉴权、AI 模型服务、业务落库）为规划能力，不计入本次原型架构。

## 4.2 前端分层架构

```text
┌──────────────────────────────────────────────────────────┐
│                      浏览器（Chrome / Edge）               │
├──────────────────────────────────────────────────────────┤
│  登录验证层  login.js                                      │
│  · 凭据校验（admin/admin123） · 登录态持久化 · 注销           │
├──────────────────────────────────────────────────────────┤
│  应用框架层  app.js                                        │
│  · 全局状态 state · 页面注册表 renderers · APP.render /     │
│    APP.navigate 路由 · 全局搜索 · 一键演示 · 全屏            │
├──────────────────────────────────────────────────────────┤
│  公共能力层  common/                                       │
│  · state.js（全局状态模型） · utils.js（工具函数）            │
│  · components.js（confirm / toast / 抽屉 / 弹窗 / 表格）    │
├──────────────────────────────────────────────────────────┤
│  业务页面层  pages/（9 个渲染器，一一对应 9 个导航模块）        │
│  dashboard / enterprise / profile / risk / graph /       │
│  workbench / project / policy / aidemo                   │
├──────────────────────────────────────────────────────────┤
│  数据层  data/mock.js（MOCK 内置演示数据集，前端唯一数据源）    │
├──────────────────────────────────────────────────────────┤
│  可视化与导出依赖  assets/vendor/                           │
│  · echarts.min.js（图表）                                 │
│  · jspdf.umd.min.js + jspdf.plugin.autotable.min.js      │
│    （企业画像 PDF 报告，矢量文字可选中）                      │
│  · pdf-font-zh.js（中文子集，导出时按需懒加载）               │
│  · 各页面 CSV 导出由 components.js 的 exportCSV 实现        │
└──────────────────────────────────────────────────────────┘
```

## 4.3 运行与路由机制

- **页面切换**：`app.js` 维护 `state.page`，通过 `APP.render()` 调用对应渲染器重绘 `#content`；页内下钻统一经 `APP.navigate(page)` 通知宿主切换路由（原型内部以 `state.page + APP.render()` 落地）。
- **直达与历史**：支持 `index.html#/页面名` 直达；页面切换写入浏览器历史（一键演示期间不写入）。
- **生命周期**：`app.js` 启动一个时钟 `setInterval` 用于演示数据动效；测试脚本以 `process.exit(0)` 结束以避免 node 常驻。
- **离线能力**：ECharts 等依赖均 vendored 于 `assets/vendor/`，`demo` 完全离线可用。

## 4.4 规划架构（原型未实现，仅作后续落地参考）

- 后端服务层（Fastify + Prisma + SQLite，见 `server/`）提供 `/api/auth/login`、`/api/bootstrap` 等真实接口；
- 前端引擎层（`web/public/engine/`）复用 demo 页面渲染器，经 `APP.sync` 钩子将操作落库；
- AI 服务层提供真实的智能体推理与证据链（demo 中智能体为预脚本确定性回答）。

---

# 五、功能菜单与信息架构

## 5.1 一级导航（取自 `demo/index.html` 的 `nav-group` / `nav-item`）

```text
招商总览
  └─ 招商驾驶舱              data-page="dashboard"

企业监管
  ├─ 企业概况                data-page="enterprise"
  ├─ 企业画像                data-page="profile"
  ├─ 风险预警                data-page="risk"
  └─ 关系图谱                data-page="graph"

招商引资
  ├─ 我的工作台              data-page="workbench"
  ├─ 招商项目                data-page="project"
  └─ 政策服务                data-page="policy"

智能应用
  └─ 招商智能体              data-page="aidemo"
```

## 5.2 全局能力（所有页面统一提供）

- **全局搜索**：顶部搜索框支持按企业 / 项目 / 政策关键词检索，结果弹窗点击可直接跳转到对应详情（企业画像 / 项目详情 / 政策详情）。
- **一键演示（演示模式）**：顶部"▶ 演示模式"按钮，按预设剧本自动串联切换各页面，再次点击"■ 停止演示"；演示期间不写入浏览器历史。
- **全屏**：顶部"全屏"按钮切换全屏展示。
- **用户区**：顶栏右侧展示账号名与角色，点击可注销。
- **页面下钻**：驾驶舱指标、企业列表、风险事件、项目、政策等均可点击下钻到对应详情或关联模块，并保持业务上下文。

---

# 六、招商驾驶舱需求（dashboard）

**模块定位**：面向主管领导的招商工作总体态势总览，是领导决策的第一入口。

招商驾驶舱共由 **1 个页面级工具栏 + 15 张功能卡片** 构成。以下按照卡片列出功能需求。

## 6.1 FR-DC-001 页面级筛选与全局操作

- **辖区筛选工具栏**：顶部"辖区"下拉（全市 + 8 个区县）切换统计口径，联动刷新全部 KPI、图表、榜单、地图与综述；右侧实时展示当前辖区口径与"共 N 家企业"。
- **导出报表**：右上"⬇ 导出报表"按钮，按当前辖区口径汇总驾驶舱关键指标导出为 CSV（`驾驶舱运行报表_*.csv`）。
- 说明：辖区切换为前端筛选（基于 `mock.js` 内置数据集），非真实行政区划权限隔离。

## 6.2 FR-DC-002 KPI 指标卡（10 项，分两行）

- 第一行（规模与风险）：企业总数（家，点击下钻企业库）、重点企业（家）、本月新增（家）、本年新增（家）、风险企业（红/橙，家，点击下钻风险预警）。
- 第二行（经济贡献）：总营收（亿元）、固定资产投资（亿元）、带动就业（人）、纳税总额（亿元）、亩均税收（万元）。
- 数据由 `mock.js` 实时聚合；除"风险企业"下钻风险页外，其余规模类卡片点击进入企业名录。

## 6.3 FR-DC-003 总体运行趋势

- 卡片含"本年度 / 近五年"切换 Tab。
- 本年度：按月营收、纳税、投资柱状 + 就业折线（双 Y 轴）；近五年：按年同构。
- 窄屏自适应（图例换行、隐藏轴名、标签不挤压，详见第十七章）。

## 6.4 FR-DC-004 重点企业营收 TOP5

- 横向条形图，按营收排序取 TOP5；企业名称完整显示（按宽度自动断行，不硬性截断）；点击下钻企业画像。

## 6.5 FR-DC-005 年度招商目标完成度

- 双环形进度：协议投资额完成率、到位资金完成率（中心数值 + 签约/到位额 vs 目标额）。
- 时序进度说明：超 / 欠时序进度 N 个百分点。

## 6.6 FR-DC-006 招商项目阶段漏斗

- 6 阶段漏斗（线索对接 → 深度洽谈 → 签约落地 → 建设推进 → 投产运营 → 达产评价）；点击下钻招商项目库。

## 6.7 FR-DC-007 全省市州对标

- 横向柱状对比全省各市州到位资金（全市口径）；标注庆阳市金额与"列全省第 N 位"。

## 6.8 FR-DC-008 区县招商业绩榜

- 横向条形：各区县签约投资额与完成率；点击下钻（切换当前辖区 / 进入企业库）。

## 6.9 FR-DC-009 重大项目 TOP5

- 列表展示：排名、项目名称、区县·责任人·阶段·进度、协议投资额；点击进入招商项目库。

## 6.10 FR-DC-010 区县企业分布热力图

- SVG 矢量地图（庆阳 8 区县），颜色深浅表示企业数量；tooltip 含企业数 / 风险企业数；点击区县切换辖区并联动全页刷新；矢量绘制，缩放 / 打印不失真。

## 6.11 FR-DC-011 风险态势

- 风险四色柱状（重大 / 较高 / 一般 / 关注）：点击按对应风险等级下钻风险预警（联动企业名录筛选）。
- 处置效能四宫格：待处置 / 处置中 / 已处置 / 闭环率。
- 近 6 个月预警走势迷你图。

## 6.12 FR-DC-012 产业分布结构

- 产业营收环图（12 个行业，按 `INDUSTRIES` 聚合）；宽屏右侧竖排图例，窄屏图例移至饼图下方、横向自动换行完整显示（扇区不叠加标签），不截断、不挤压（详见第十七章响应式说明）。

## 6.13 FR-DC-013 政策兑现服务

- 三项指标：年内已兑现（亿元）、兑现率、惠企企业（家）。
- TOP 兑现政策进度条（逐项兑现率）；注脚展示年度安排资金与惠及企业数（取自 `POLICY_REDEEM` 真实推导）。

## 6.14 FR-DC-014 ✦ 今日运行综述

- 基于当前辖区数据自动生成的 AI 风格综述列表（含企业总体、风险、招商推进等动态条目），序号徽标呈现。

## 6.15 FR-DC-015 ⚡ 今日行动项

- 行动建议清单：红/橙级预警待处置、逾期服务任务、在谈项目储备、完成率最低区县、时序进度超/欠；按优先级以颜色圆点区分。

## 6.16 FR-DC-016 数据资源接入 · 来源分布

- 数据来源列表（取自 `M.DATA_SOURCES`：名称 + 更新频度 + 色点标识）；注脚：共接入 N 类数据来源，覆盖工商、税务、社保、司法、知识产权、生态环境、应急管理等核心领域。
- 说明：此为 demo 内置演示数据集的"来源构成"展示，非真实数据接入链路（真实接入为规划能力，见第十八章）。

---

# 七、企业概况需求（enterprise）

**模块定位**：企业中心——企业名录与档案查询，是进入企业画像的入口。

## 7.1 FR-ENT-001 企业名录

**顶部统计数据（随筛选实时联动）**：页面顶部统计条（overview-strip）展示当前筛选口径下的聚合指标，共 8 项：筛选企业数（家）、总营收（亿元）、纳税总额（亿元）、带动就业（人）、风险企业（红/橙·家）、大型企业（家）、近30天新签约（家）、平均履约率（%）。

**查询与筛选**：关键词（企业名称 / 法定代表人 / 统一社会信用代码）、风险等级、行业、区县、规模、企业标签六类条件；支持一键清空筛选条件。

**企业名录表格**：表头共 13 列（取自 `COLUMNS` 列配置）。其中"企业名称"为固定必选列（不可隐藏），其余 12 列可通过"⚙ 列设置"自定义显隐；系统默认显示 8 列。

| 序号 | 列字段 | 说明 | 默认显示 |
| --- | --- | --- | --- |
| 1 | 企业名称 | 企业全称（固定列，不可隐藏） | 是 |
| 2 | 企业标签 | 企业标签（如龙头 / 高新 / 专精特新等） | 否 |
| 3 | 规模 | 企业规模层级 | 是 |
| 4 | 信用状态 | 信用状态 | 是 |
| 5 | 营收 | 营收（亿元） | 是 |
| 6 | 纳税 | 纳税（亿元） | 是 |
| 7 | 员工数 | 员工人数 | 是 |
| 8 | 履约率 | 履约率（%） | 是 |
| 9 | 风险等级 | 风险等级（红 / 橙 / 黄 / 蓝四色徽标） | 是 |
| 10 | 法定代表人 | 法定代表人 | 否 |
| 11 | 注册资本 | 注册资本 | 否 |
| 12 | 成立日期 | 成立日期 | 否 |
| 13 | 匹配政策数 | 匹配政策数量 | 否 |

**表格能力**：点击列头排序（营收、纳税、员工数、风险指数、履约率的升 / 降序）；分页（每页 10 条，显示"共 N 家 · 第 X/Y 页"）；"⚙ 列设置"弹窗勾选显示列；行勾选 + "⚖ 对比"进入企业对比浮窗（并排对照多家企业的经营与风险指标）；"⬇ 导出报表"按当前筛选导出企业数据 CSV（`企业概况_*.csv`，列含企业名称 / 统一社会信用代码 / 法定代表人 / 行业 / 区县 / 规模 / 标签 / 信用状态 / 营收 / 纳税 / 员工数 / 履约率 / 风险等级 / 风险指数）。

**下钻与批量**：行点击进入企业画像（下钻）；勾选复选框可执行批量操作；"✕ 清空"清除已选对比企业。

## 7.2 FR-ENT-002 企业档案（下钻、对比与服务入口）

企业概况页的"企业档案"以**下钻 + 对比浮窗 + 服务入口**三种形式落地，不再单独渲染一个静态档案面板：

- **行点击下钻**：名录表格任意一行点击 → 进入该企业的 360° 画像（第八章），携带 `state.ent` 与当前筛选上下文，画像页左侧列表自动定位并居中该企业。
- **企业对比浮窗（⚖ 对比）**：勾选 2–5 家企业（最多 5 家）后，顶部"⚖ 对比"按钮打开对比浮窗，按 **4 个维度组**并排对照，每组内逐指标标注最优/最差：
  - **基本信息**：所属行业、所在区县、企业规模、法定代表人、成立日期、注册资本（亿元/万元自动格式化）。
  - **经营概况**：年营收（≥1 亿显示亿元，否则万元）、年纳税（万元）、员工数、用地面积（亩）、利润。
  - **经营状态**：经营状态、信用状态、履约率（%）。
  - **风险情况**：综合风险分、**风险等级**（色徽）、匹配政策（项，取自 `e.policies.length`）。
  - 数值型行（含"越高越好"标记者）在多家中取最大值标绿、最小值标红；风险类（越高越差）反向标注。
- **✕ 清空**：清空已勾选的对比企业集合（`state.compareEntIds`），浮窗关闭。
- **＋ 企业服务（服务入口）**：出现在企业画像头部（见 8.1），调用 `APP.openTaskForm({entId, type:'企业服务', title:'企业走访服务：XX'})` 为企业创建"企业走访服务"任务，联动工作台（第十一章）。
- **导出**：⚖ 对比与名录的"⬇ 导出报表"共用 `components.js` 的 `exportCSV`，导出列见 7.1（14 列）。

## 7.3 全局操作

- **导出报表**：按当前筛选导出企业数据 CSV（`企业概况_*.csv`）。

---

# 八、企业画像需求（profile）

**模块定位**：单个企业的 360° 画像。页面采用"左侧企业列表（带筛选）+ 右侧详情（企业头部 + 六层页签 + AI 政策智能匹配卡）"结构，全部数据来自 `M.ENTERPRISES` 单条企业对象（`entById(state.ent)`）。

## 8.1 FR-PRO-001 页面框架：左侧列表 + 企业头部 + 六层页签

- **左侧企业列表（带筛选）**：
  - 顶部搜索框（企业名称，动态筛选 + 中文输入法 IME 保护，重渲染后恢复光标位置）。
  - 两个筛选下拉："全部行业"（`M.INDUSTRIES`，12 项）、"全部风险"（重大/较高/一般/关注）。
  - 列表项：企业名 + 规模 + 风险色徽 + 前 2 个标签；按综合风险分降序；底部"共 N 家企业"。
  - 点击列表项 → 切换当前企业（`state.ent`），重置到第 0 页签，保留列表滚动位置（外部进入时自动居中选中项）。
- **企业头部信息网格（8 字段）**：统一社会信用代码（`wide`）、法定代表人、成立时间、注册资本（`overview.regCapital`，≥1 亿显示亿元）、企业规模、所属行业（`wide`）、经营状态（`status.biz`）、注册地址（`wide`）；姓名首字 Logo + 标签区。
- **右侧企业侧栏**：综合风险指数大数（`e.riskScore`，按 `M.LEVELS[level].color` 着色）+ 风险等级色徽 + "＋ 企业服务"按钮（调用 `APP.openTaskForm`，见 7.2）。
- **六层页签**：概况 / 经营状态 / 经营趋势 / 关系网络 / 企业风险 / AI 综合研判（索引 0–5）；页签切换仅显隐对应 `tab-pane`，经营趋势页签激活时延迟重绘趋势图。

## 8.2 FR-PRO-002 概况页签

- **顶部 4 KPI**：总营收（`overview.revenue`，≥1 亿显示亿元）、纳税（`overview.tax` 万元）、员工数（`overview.employees.toLocaleString()+'人'`）、履约率（`status.performRate%`，≥90 绿 / ≥70 橙 / 否则红）。
- **招商承诺完成情况表**：列＝指标 / 承诺 / 实际 / 进度条 / 完成率%。每行来自 `e.commitments`（如营收、纳税、就业岗位），完成率 = `actual/promise`，按阈值着色（≥90 绿、≥70 橙、否则红）。

## 8.3 FR-PRO-003 经营状态页签

- **顶部 3 KPI 卡**：经营状态（`status.biz`，正常绿/关注橙/异常红）、信用状态（`status.credit`，同色规则）、企业规模（`scale`）。
- **企业动态时间轴**（`.timeline`）：逐条 `e.dynamics`（日期 + 类型徽标 + 文本），按时间倒序。

## 8.4 FR-PRO-004 经营趋势页签

- 单个趋势图（`#c_profile_trend`，高 320px），取自 `e.operation`（近 6 期营收/纳税/投资/员工数组，单位亿/万元）。窄屏自适应不挤压（见第十七章）。

## 8.5 FR-PRO-005 关系网络页签

- **股东结构表**：列＝股东名称 / 持股比例（%）/ 比例条；来自 `e.shareholders`（名称 + 比例）。
- 底部引导语："完整关系网络请查看「产业关系图谱」模块"（跳转 `graph`）。

## 8.6 FR-PRO-006 企业风险页签

- **风险维度矩阵**（`.risk-matrix`）：对 8 个启用维度（`RISK_DIMS` 中 `weight>0`），逐维展示维度名 + 权重%（`Math.round(weight*100)`）+ 维度得分（`e.risks[key]`）+ 色条（≥65 红 / ≥45 橙 / ≥25 黄 / 否则蓝）；评估完成后额外标注"档案值 X"与变化 `↑/↓`。
- **操作按钮（右对齐）**：
  - "✦ AI 风险评估"（`APP.runProfileRiskAssess`）：弹出多步研判对话框（见 8.6.1），完成后刷新本页签矩阵数据（差异标注），报告仅在弹窗内呈现。
  - "查看风险事件 →"（`APP.goRisk({keyword:企业名, entId})`）：跳转风险预警页并携带企业关键词/ID。

### 8.6.1 AI 风险评估对话框（模拟智能体多步研判）

- 对话框（宽 640px，正文固定 `48vh`/最小 380px），标题"✦ AI 风险评估 · 企业名"，含"已用时 N 秒 · P%"进度指示与关闭按钮（Esc/点遮罩关闭）。
- **三步清单**（逐步点亮：完成✓ / 进行中动画 / 等待○）：① 汇聚经营/财务/司法/信用等多维数据；② 比对行业基准、区域水平与企业历史趋势；③ AI 模型推理生成风险评估结论。
- **过程日志区**（控制台观感，自动滚动）：逐步打印初始化、工商登记/经营数据/司法信用/招投标/知产核验、8 维就绪、行业基准、区域水平、历史对比（首次深度评估标注）、特征融合、推理完成（综合指数 + 等级）、生成建议；总计约 8.6 秒。
- **结果写回**：完成后将本轮评估（`buildAssessResult`，基于档案值在 ±8 内小幅波动，置信度 86–95，含综合指数/等级/维度/主要风险点/处置建议）写入页签矩阵，并弹出评估报告（置信度 + 时间、维度评分更新表、主要风险点、处置建议、档案值 vs 本次评估对比）。
- 处置建议按维度映射到 `ADVICE_MAP`（operation/finance/judicial/credit/tender/tax/perform/ip）。

## 8.7 FR-PRO-007 AI 综合研判页签

- **研判面板**（`.ai-panel`）：标题"✦ AI 综合研判报告" + 置信度与时间；段落：研判摘要、核心优势（列表）、关注风险（列表）、数据来源（可溯源徽标）、历史对比（上次研判 / 本次研判 / 变化 `↑/↓`）。
- **初始态**：深度企业（`e.ai` 非空）展示完整面板；非深度企业展示简版（置信度 82%，引导"重新分析研判"或纳入重点库）。
- **操作按钮（右对齐）**：
  - "⟳ 重新分析研判"（`APP.runProfileJudge`）：弹出 11 步全面研判对话框（见 8.7.1），完成后刷新本页签为完整报告，会话内有效、刷新还原。
  - "⬇ 导出报告"（`APP.exportJudgeReport`）：导出正式商务版式 A4 PDF（见 16.2，矢量中文可选取）。

### 8.7.1 AI 综合研判对话框（11 步全面体检）

- 对话框（宽 680px，正文固定 `58vh`/最小 420px），11 步单步展示（运行中旋转环、完成定格✓），进度指示。
- **11 步**：核验基本信息与主体资格 → 解析股权结构与关联网络 → 分析经营表现与履约水平 → 扫描招投标参与 → 盘点知识产权 → 剖析客户与供应商 → 比对同业竞争 → 研判行业环境 → 分析投融资信贷 → 评估多维风险敞口（八维加权）→ 生成综合研判结论。每步伴随详细日志（含统一社会信用代码、营收/纳税/员工、招投标中标率、专利/软著/商标、客户集中度、授信/贷款余额、八维得分、综合指数等真实字段）。总计约 42 秒。
- **报告内容**：核心优势（注册资本/规模、行业区位、营收纳税、中标率、知识产权储备、招商承诺完成率）、关注风险（最高两维 + 客户集中度/知产纠纷/贷款偏高/信用状态 + 持续跟踪建议）、数据来源（工商/税务/司法/招投标/知识产权/社保/动态监测/投融资 + 企业自有来源）、置信度（88–96）、历史研判对比。
- 关联数据为参数化模拟集 `SIM_POOL`（客户/供应商/同业/投资方/行业景气度/政策），按企业 id 稳定生成，纯虚构演示。

## 8.8 FR-PRO-008 AI 政策智能匹配卡

- 位于六层页签下方独立卡片：标题"AI 政策智能匹配"，副标题"基于企业画像自动匹配 N 项政策"（`(e.policies||[]).length`）。
- 政策列表项（最多 6 条）：政策名 + 发布部门 + 层级 + 适用（`item.apply`）+ "查看政策详情 →"；点击项（`data-policy-id`）→ 设置 `state.policyId`/`policyLocateOnly=false` 并跳转政策服务页打开详情抽屉（见 13.3）。

---

# 九、风险预警需求（risk）

**模块定位**：风险综合评价（双视图）、风险事件清单、派发处置与三态闭环。全部数据来自 `M.RISK_EVENTS` / `M.ENTERPRISES` / `M.RISK_DIMS`。

## 9.1 FR-RISK-001 双视图：全市态势 + 选中企业对比

> **八大风险维度（统一口径）**：综合风险指数由以下 8 个维度按权重加权求和得到（满分 100，权重合计 = 1.0），维度名与权重取自 `M.RISK_DIMS`（`mock.js` 第 54 行，与第九章风险模型、第十三章政策匹配口径一致）。下表维度释义为业务语义展开，代码侧仅固化 `key / name / weight` 三项。

| 序号 | 维度 key | 维度名 | 权重 | 业务释义（招商监管语境） |
| --- | --- | --- | --- | --- |
| 1 | `operation` | 经营风险 | 0.20 | 企业存续与经营稳定性：经营异常、停业、注销、地址失联、抽查不合格等工商经营状态异常信号 |
| 2 | `finance` | 财务风险 | 0.15 | 资产负债、现金流、盈利能力等财务健康度异常，或财务信息披露异常 |
| 3 | `judicial` | 司法风险 | 0.15 | 涉诉、被执行、失信、裁判文书、司法冻结等司法负面记录 |
| 4 | `credit` | 信用风险 | 0.15 | 失信被执行人、行政处罚、欠税公告、信用中国负面等信用惩戒记录 |
| 5 | `tender` | 招投标风险 | 0.10 | 招投标违规、串标、废标、被限制投标等公共资源交易异常 |
| 6 | `tax` | 税务风险 | 0.10 | 欠税、税务违法、非正常户、稽查处罚等税务合规异常 |
| 7 | `perform` | 招商履约风险 | 0.10 | 招商承诺（投资强度、产值、税收、用工）未按约定履约、项目停滞、退出等履约偏差 |
| 8 | `ip` | 知识产权风险 | 0.05 | 专利无效/纠纷、商标侵权、核心技术外流、知识产权质押异常等 |

- **计分口径**：单企业综合风险分 `riskScore = Σ(weight_i × risks[dim_i])`（`calcRiskScore`，`mock.js` 第 72 行），各维度分值区间 0–100；等级阈值 `scoreToLevel`（`mock.js` 第 79 行）：≥65 重大（红）/ ≥45 较高（橙）/ ≥25 一般（黄）/ 其余 关注（蓝）。雷达图 `indicator.max = 100` 即对应此 0–100 分值区间。

- **左卡「全市风险态势」**：标题副文"全市 N 家企业加权平均"。含左侧雷达图（`#c_risk_radar_all`，高 300px，8 维 indicator max=100）+ 右侧综合风险大数（`avgScore` 按等级着色）+ 等级色徽 + 8 维得分列表（维度名 : 平均分）。
- **右卡「企业风险态势」**：标题栏含企业下拉（`#riskSelEnt`，取风险分 Top50 企业）；左侧雷达（`#c_risk_radar_sel`，叠加"平台"虚线对比）+ 右侧该企业综合分与等级 + 8 维得分（对比全市平均，标注 `↑/↓/持平`，升红降绿）+ "查看企业画像"按钮（`APP.viewEnterprise`）。
- **统计卡行（8 项，两行 ×4）**：重大风险企业（红）/ 较高风险企业（橙）/ 一般风险企业（黄）/ 关注风险企业（蓝，来自 `M.riskStats()`）/ 风险事件总数 / 待处置 / 处置中（=已派发）/ 已处置（=已关闭）。

## 9.2 FR-RISK-002 维度分布 + 近 30 天趋势

- **风险维度分布**（横向柱，`#c_dim_bar`，高 280px）：8 维事件计数（`dimStats`），渐变蓝柱 + 数值标签。
- **近 30 天风险事件趋势**（折线面积，`#c_risk_trend`，高 280px）：按日聚合事件数（伪随机日增量，红渐变面积），x 轴间隔显示避免拥挤。

## 9.3 FR-RISK-003 风险事件清单（筛选 + 表格 + 导出）

- **顶部工具栏按钮**："＋ 新建风险事件"（`openRiskForm`）、"批量派发"（`#batchDispatch`，演示提示"已发起批量派发，共 0 条（演示）"）、"⬇ 导出报表"（`#exportRisk`）。
- **筛选卡（5 项）**：关键词（企业/事件标题，动态 + IME 保护）/ 风险等级（全部+重大/较高/一般/关注）/ 风险维度（8 维）/ 状态（全部+待处置/已派发/已关闭）/ 区县（全部区县 + 8 区县）。任一变更即时重渲染；空态"清除筛选"恢复。
- **表格列字段定义表（7 列，含首列）**：

| 列序 | 列字段 | 说明（含数据来源） | 默认显示 |
| --- | --- | --- | --- |
| 1 | 选择（复选框） | 勾选行，配合"批量派发"工具栏按钮；表头含全选框 | 是 |
| 2 | 风险等级 | 四色等级色徽（`M.LEVELS[level].name`：重大红 / 较高橙 / 一般黄 / 关注蓝） | 是 |
| 3 | 风险事件 | 事件标题（`ev.title`）；副行显示发生时间（`ev.time`） | 是 |
| 4 | 风险维度 | 维度中文名（`ev.dimName`，对应 9.1 八大维度之一） | 是 |
| 5 | 涉事企业 | 企业名称（`ev.enterprise`），蓝字可点 → 回填 `state.ent` 并下钻企业画像（`ev.entId`） | 是 |
| 6 | 状态 | 三态色徽：待处置 `s-red` / 已派发 `s-orange` / 已关闭 `s-green` | 是 |
| 7 | 操作 | 按状态给出下一步动作：待处置→"派发"、已派发→"跟进"(`APP.goDispatch`)、已关闭→"查看" | 是 |

- **底层数据字段（CSV 导出 `风险事件清单_日期.csv`，8 列）**：事件ID(`ev.id`) / 风险等级(`M.LEVELS[level].name`) / 风险维度(`ev.dimName`) / 事件标题(`ev.title`) / 涉事企业(`ev.enterprise`) / 时间(`ev.time`) / 状态(`ev.status`) / 详情(`ev.detail`)。
- **分页**：每页 10 条（`PS=10`），"共 N 条风险事件 · 第 X/Y 页"，行点击打开风险详情抽屉；涉事企业点击回填 `state.ent` 并刷新。
- **导出 CSV（8 列）**：事件ID / 风险等级(`M.LEVELS[name]`) / 风险维度 / 事件标题 / 涉事企业 / 时间 / 状态 / 详情（`ev.detail`），文件名 `风险事件清单_日期.csv`。

## 9.4 FR-RISK-004 风险处置（派发）与详情

- **风险详情抽屉**（`showRiskDetail`，宽 420）：展示事件标题、事件ID、等级色徽、状态色徽、风险维度、涉事企业（可点下钻）、发现时间、所在区县、事件详情、处置建议；底部按钮：
  - 待处置 → "派发处置"（`APP.handleDispatch`，打开派发抽屉，状态→已派发）；
  - 非已关闭 → "记录处置进展"（演示占位）；
  - 始终 → "关闭"。
- **新建风险表单**（`openRiskForm`，宽 480）：字段＝涉事企业（下拉全量）/ 风险维度（8 维）/ 风险等级（4 级）/ 事件标题（必填）/ 风险详情（必填 textarea）/ 处置建议（选填 textarea）；保存时校验企业+标题+详情非空，生成新 ID（`R`+序号），`status:'待处置'`，`daysAgo:0`，`unshift` 至 `M.RISK_EVENTS` 顶部并重渲染。
- 注：派发/落库为**原型前端演示**，真实任务落库为规划能力（见第十八章）。

## 9.5 FR-RISK-005 风险闭环（三态模型）

- 风险事件三态：`待处置 → 已派发 → 已关闭`，与风险处置任务（`TASKS`，`type:'风险处置'`）**一一对应**：已派发事件 ↔ 未完成任务（待处理/进行中/已逾期），已关闭事件 ↔ 已完成任务（含完成时间，晚于事件发现日），待处置事件未派发不生成任务（数量守恒、时序自洽，由 `tools/check_invariants.js` 校验）。
- 状态配色：待处置 `s-red`、已派发 `s-orange`、已关闭 `s-green`（页面指标同步显示为"处置中=已派发、已处置=已关闭"）。

---

# 十、关系图谱需求（graph）

**模块定位**：产业/企业关系网络可视化与交互探索。

## 10.1 FR-GRAPH-001 图谱可视化与交互

关系图谱采用 ECharts 力导向图（graph）实现，页面由**顶部"图谱概览"KPI 卡 + 左侧"图谱导航"控制面板 + 右侧"企业关联关系图谱"主卡**三部分组成。以下完整描述其统计、菜单、选项、按钮、图例与交互，可据以复现图谱卡片。

### 10.1.1 顶部统计（图谱概览卡）

卡片标题"图谱概览"，副标题随区县筛选联动（默认"全量企业关联关系总览"，筛选某区县则显示"当前范围：XX"）。KPI 共 10 项，随区县筛选实时重算：企业节点（家）、投资平台（个）、政府部门（个）、匹配政策（条）、招商线索（个）、外部市场（个）、涉及行业（个）、覆盖区县（个）、红橙风险（家，红色 + 橙色数字）、平均关联（条/家 = 范围内关系数 / 企业数）。

### 10.1.2 左侧"图谱导航"控制面板

- **区县筛选**：下拉框（全部区县 + 8 个区县），切换图谱与顶部 KPI 的统计范围。
- **企业搜索**：文本输入框 + "定位"按钮（回车亦可）。支持节点名称模糊匹配；定位后进入聚焦模式（暗化其他节点、放大目标及其邻居）；若目标不在当前筛选区县或所在图层已关闭，给出橙色提示；点击"重置视图"退出聚焦。
- **着色维度**：分段控件（按行业 / 按区县），切换节点分类着色口径。
- **布局方式**：分段控件（力导向 / 环形 / 树形）；树形以"庆阳产业投资集团"为根做放射状层次布局。
- **节点图层**：6 个图层开关（点击切换显隐）——企业节点、投资平台、政府部门、匹配政策、招商线索、外部市场；其中"匹配政策"默认关闭（连线最密，避免过密）。
- **连线样式**：
  - "按业务语义着色"勾选框（关闭则连线统一浅灰实线）；
  - 关系类型筛选：8 类连线勾选框（见 10.1.4），可逐类显隐；系统强制至少保留一类（否则提示"至少保留一类连线"）。
- **图例**：节点分类色点（按行业 12 色 / 按区县 8 色 + 辅助类别：投资平台、外部市场、政府部门、匹配政策、招商线索）。
- **操作说明**：图标含义（节点大小 = 企业规模；🏗/💰 = 土地 / 资金缺口；红边 = 风险企业；绿虚框 = 在谈招商线索）、图层切换、路径分析、鼠标悬停、鼠标操作（单击进入画像 / 双击仅看关联 / 双击空白恢复 / 滚轮缩放）。

### 10.1.3 右侧图谱主卡（标题栏按钮 + 画布）

标题栏右侧控件：

- **风险透视**：开关胶囊（高亮红 / 橙风险企业及关联链路，⚠ 标注一级风险关联，其余节点暗化）。
- **路径分析**：开关胶囊（开启后依次点击起、止节点，自动 BFS 计算并高亮最短关联路径；与聚焦模式互斥）。
- **缩小（－）/ 放大（＋）**：按固定步进（×1.18 / ÷1.18）调整缩放比例，跨重绘保持，区间 [0.3, 5]。
- **重置视图**：清空隔离 / 聚焦 / 风险透视 / 路径分析 / 图层 / 区县 / 搜索等全部状态，销毁并重建图表实例回到默认。
- **导出图片**：将当前图谱导出为 PNG（`企业关联关系图谱.png`，pixelRatio 2，白底；图表未就绪时提示"导出失败：图表未就绪"）。

画布：力导向图，高度 660px；内置仅保留平移（roam=move），缩放由手写滚轮 / 双指控制（灵敏度约 4%/格，约为默认 1/3），节点可拖拽。

### 10.1.4 图例（连线 8 类业务语义：颜色 × 线型）

| 关系类型 | 颜色 | 线型 | 包含关系 |
| --- | --- | --- | --- |
| 政策服务 | #2563EB | 实线 | 政策匹配 |
| 招商线索 | #22C55E | 实线 | 招商洽谈、平台对接 |
| 要素保障 | #2563EB | 点线 | 招商服务、要素保障、数字化赋能 |
| 政府监管 | #F59E0B | 虚线 | 日常监管、重点监管 |
| 同一法人 | #F97316 | 虚线 | 同一法人 |
| 投资合作 | #8B5CF6 | 实线 | 参股、控股、合作 |
| 市场协作 | #06B6D4 | 实线 | 服务、供应、出口、物流配套 |
| 产业链 | #CBD5E1 | 实线 | 同产业、供应链、竞争 |

未匹配上表的其余关系归入"产业关联"（#CBD5E1 实线）。

### 10.1.5 交互行为

- **悬停提示**：企业节点含行业 · 区县 · 营收 · 风险等级及土地 / 资金缺口；辅助节点含"关联节点 N 个"；连线含关系语义与两端节点名，风险链路额外标红"风险传导路径"。
- **单击节点**：路径分析模式下为选点；红 / 橙风险企业 → 聚焦并弹出"风险影响半径"浮卡；其余企业 → 进入企业画像；招商线索节点 → 跳转招商项目页。
- **双击节点**：进入隔离模式（仅显示该节点及其直接邻居），再次双击同一节点或双击空白恢复。
- **风险影响半径浮卡**（可拖动，右上角）：展示一级关联家数、其中红 / 橙数、二级关联家数、关联明细（按风险排序，可点击下钻画像）、行业 / 区县分布、处置建议，并提供"进入画像 / 关闭"按钮。
- **路径分析浮条 + 路径卡**（可拖动，左上角浮条 / 右上角卡）：显示起点 → 终点最短路径步数及逐步节点与关系，支持"清除路径"。
- **底部统计**：实时显示"当前显示 N 个节点 / M 条关系"，并标注是否处于聚焦 / 风险透视模式及一级风险关联家数。

## 10.2 FR-GRAPH-002 图谱 KPI 与导出

- 图谱 KPI（见 10.1.1）：节点 / 图层 / 行业 / 区县 / 风险 / 平均关联等统计，随区县筛选联动。
- **导出图片**：将当前图谱视图导出为 PNG（图表就绪校验，失败提示"导出失败：图表未就绪"）。

---

# 十一、我的工作台需求（workbench）

**模块定位**：招商人员的任务中心与每日工作入口。

## 11.1 FR-WB-001 今日待办与动态

**顶部统计（KPI 网格，共 8 项）**：我的任务（件）、待处理（件）、进行中（件）、已逾期（件）、已完成（件）、完成率（%）、本周新增（件）、负责企业（家，全部任务关联企业去重数）。

**今日待办清单**：按当日日期（deadline == 今天且未完成）取前 5 条，每条含优先级彩色圆点、任务标题、关联企业 / 类型副文本，点击进入任务处理；当日无待办时显示"今日无待办任务 🎉"。

**任务类型分布**：按类型（风险处置 / 项目跟进 / 企业服务 / 政策推送 / 日常巡检）聚合的饼图。

**最近动态**：动态流（系统最近产生的任务 / 处置 / 项目等动态）。

**任务清单表格**：支持按任务状态、任务类型、优先级筛选与"重置筛选"。表格列字段如下（取自渲染表头与 CSV 导出字段）：

| 渲染列 | 字段说明 |
| --- | --- |
| 选择（复选框） | 勾选任务，配合"批量完成" |
| 优先级 | 高 / 中 / 低优先级彩色徽标 |
| 任务名称 | 任务标题；副行显示"类型 · 关联企业" |
| 截止日期 | 截止日期；逾期标红并提示逾期天数 |
| 状态 | 待处理 / 进行中 / 已逾期 / 已完成 |
| 操作 | 处理 / 完成 / 查看（按状态给出下一步动作） |

底层数据字段（CSV 导出 `任务清单` 列）：编号、任务名称、类型、关联企业、优先级、截止日期、状态、创建时间。

## 11.2 FR-WB-002 任务办理

- 新增任务（抽屉表单：企业、类型、标题、优先级、截止日期等）。
- 批量完成（选中任务批量标记完成）。
- 任务详情抽屉：阶段备注、办理、关联企业。

## 11.3 全局操作

- **导出**：导出任务清单 CSV。

---

# 十二、招商项目需求（project）

**模块定位**：招商项目全生命周期管理。

## 12.1 FR-PROJ-001 项目阶段漏斗与趋势

- 项目阶段漏斗（**6 阶段**：线索对接 → 深度洽谈 → 签约落地 → 建设推进 → 投产运营 → 达产评价）。
- 近 6 月项目落地趋势。
- 项目清单：按阶段、关键词筛选。

## 12.2 FR-PROJ-002 项目详情与推进

**顶部统计（KPI 网格，共 8 项）**：项目总数（个）、总投资额（亿元）、签约落地（个）、建设推进（个）、投产运营（个）、达产评价（个）、本月对接（个）、平均落地周期（天，已签约项目"线索对接 → 签约落地"真实计算）。

**项目清单**：卡片网格 + 分页（每页 10 个），支持按阶段、所在区县、责任单位、关键词（逐字动态，兼容中文输入法）筛选；"⬇ 导出"按当前筛选导出全部项目 CSV。项目清单字段（取 CSV 导出列）如下：

| 序号 | 字段 | 说明 |
| --- | --- | --- |
| 1 | 编号 | 项目 ID |
| 2 | 项目名称 | 项目名称 |
| 3 | 关联企业 | 关联企业名称 |
| 4 | 阶段 | 6 阶段之一（线索对接 / 深度洽谈 / 签约落地 / 建设推进 / 投产运营 / 达产评价） |
| 5 | 所在区县 | 项目所在区县 |
| 6 | 责任单位 | 责任单位 |
| 7 | 总投资(万元) | 总投资额 |
| 8 | 进度(%) | 当前推进进度 |
| 9 | 风险 | 项目风险等级 |

卡片正面展示：项目名称、关联企业、阶段、所在区县、总投资、当前进度。

**项目详情抽屉**：点击项目卡片打开，展示投资、当前进度、关键节点时间轴（历史阶段只读回显、当前阶段可编辑、未来阶段未开始）；可记录本阶段推进情况 / 堵点 / 下步安排（"保存进展"就地更新抽屉内状态与进度步骤条）；展示责任单位、所在区县、总投资、进度等明细。

**新建项目**："＋ 新建项目"面向**招商引进新企业**——手动输入拟引进企业全称（不从已有企业中选择），输入过程**实时查重**：名称已存在于企业名单时内联红字提示且无法创建，仅新企业允许建档。同时填写项目名称（留空按"企业名 + 项目"自动生成）、投资总额（必填）、责任单位、所在区县、所属行业、联系人及**线索对接进展情况**。创建成功后**新企业同步写入企业名单**（企业概况页可查，带"新引进"标签、初始蓝色风险），新项目置顶显示（初始阶段：线索对接，进度 5%，自动生成时间线与对接记录）并以蓝底高亮定位。

**项目清单工具栏**："＋ 新建项目"、"✦ AI 智能推荐"（见 12.3）、"⬇ 导出"按当前筛选导出全部项目 CSV。

**AI 分析**：项目清单工具栏的 AI 智能推荐入口（见 12.3，原型为确定性评分）。

## 12.3 FR-PROJ-003 AI 智能推荐招商项目（原型形态）

- **入口**：项目清单工具栏"✦ AI 智能推荐"按钮（非项目详情内）。候选池取自招商目标（在谈）企业库 `PROSPECT_ENTERPRISES`，每次从未推荐的候选中顺序取 3–5 家；候选全部推荐完后提示"当前候选企业已全部完成推荐"。
- **对话框**：标题"✦ AI 招商项目智能推荐"，副标题"基于庆阳市产业政策与企业画像的智能匹配引擎"；先以流式日志展示"🧠 AI 推荐引擎运行中…"的模拟推理过程，完成后提示"✓ 推荐完成，共筛选出 N 家优质目标企业"，随后逐家输出推荐卡：企业名称 + 综合评分大数（≥90 绿 / ≥80 蓝 / 其余橙）+ 所属行业 / 企业规模 / 意向投资等字段 + 推荐理由（**投资强度约 30%**：如"投资规模超 15 亿元，重大产业项目"；**产业契合度约 25%**：与庆阳市重点招商方向比对；另结合区县布局、承接条件等维度），评分与理由由候选企业数据确定性推导。
- **确认纳入招商项目**：点"确认纳入招商项目"后，本次推荐的候选企业批量写入项目库（初始阶段线索对接），推荐计数递增、同批候选不再重复推荐；底部"关闭"。
- 说明：原型为**基于数据的确定性评分**，非真实大模型调用；真实 AI 推理为规划能力（见 18.3）。

---

# 十三、政策服务需求（policy）

**模块定位**：政策库、AI 智能匹配与政策服务闭环（申报/推送/兑现）。数据来自 `M.POLICY_LIB` / `M.POLICY_REDEEM` / `M.POLICY_PENDING_POOL`。

## 13.1 FR-POL-001 顶部 8 KPI + 左侧分类 + 近期新政策

- **顶部 8 KPI（来自 `POLICY_REDEEM` 真实推导）**：政策总数（`P.length`）/ 国家级（红）/ 省级（橙）/ 市级（蓝）/ 年度安排（`R.planWan/10000` 亿元）/ 已兑现金额（`R.redeemedWan/10000` 亿元）/ 总体兑现率（`R.rate%` 绿）/ 惠及企业（`R.entsHelped` 家）。
- **左侧政策类型侧栏**（`.p-side`）："全部政策" + 各类型计数（按 `type` 聚合）；点击切换 `state.filter.policy.type`。"近期新政策 Top5"：按 `date` 倒序取前 5，展示政策名 + 部门 + 日期，点击下钻详情。

## 13.2 FR-POL-002 政策列表（筛选 + 卡片 + 工具栏）

- **工具栏按钮**："⟳ 同步政策"（`#polSync`，见 13.5）、"✦ AI 智能匹配"（`#polAiMatch`，见 13.4）、"⬇ 导出报表"（`#polExport`）。
- **筛选卡（3 项）**：搜索政策（政策名/发布部门，回车或动态 + IME 保护）/ 政策层级（全部+国家级/省级/市级/区县）/ 发布部门（全部 + 去重部门下拉）。
- **政策卡片字段定义表**（列表采用卡片网格呈现，每页 8 条，`PS=8`，与 demo 一致）：

| 卡片字段 | 说明（含数据来源） |
| --- | --- |
| 层级色标 | 国家级红 / 省级橙 / 市级蓝 / 区县绿（取 `p.level`） |
| 类型 | 政策类型标签（`p.type`） |
| 标题 | 政策名称（`p.title`） |
| 适用 / 摘要 | 适用对象或摘要（`apply | | brief`，左值优先） |
| 发布日期 | 发布日期（`p.date`，📅 图标） |
| 发布部门 | 发布部门（`p.dept`，🏛️ 图标） |
| 浏览热度 | 浏览热度（`60 + (序号*13)%90`，🔥 图标，稳定不跳变） |
| 匹配企业数 | 智能匹配企业数量（`p.entCount`） |
| 操作入口 | "查看详情 →"，点击卡片（`data-id`）或近期政策打开详情抽屉（见 13.3） |

- **底层数据字段（CSV 导出 `政策清单`，8 列）**：编号 / 政策名称 / 层级 / 类型 / 发布部门 / 匹配企业数 / 年度安排(万元) / 已兑现(万元)（后两列来自 `POLICY_REDEEM` 的 `planWan`/`rate` 推导，详见第十五章）。
- **分页**："共 N 条政策 · 第 X/Y 页"。

## 13.3 FR-POL-003 政策详情抽屉

- 由 `showPolicyDetail` 渲染（宽 460），展示：标题、发布部门·层级·日期、政策摘要（`brief||apply`）、适用对象（`apply`）、支持方式（`support`，默认"资金补贴、税收优惠、项目扶持等"）、申报材料（`materials`，默认三段通用文本）、智能匹配企业（按 `e.policies` 反查，最多 6 家，列名称 + 所属行业；点击企业 → 关闭抽屉并下钻画像）、关闭按钮。
- **服务闭环（数据联动，非抽屉按钮）**：企业画像"AI 政策智能匹配"卡片展示的匹配项数（`e.policies.length`）即来源于此；"✦ AI 智能匹配"确认后回写企业 `policies`（见 13.4）；"⟳ 同步政策"按标签回写匹配企业（见 13.5）。详情抽屉当前渲染"关闭"按钮，申报/推送任务创建（`APP.createTask`，`type:'政策推送'`）在代码路径中预留，作为服务闭环的落库能力（规划/演示）。

## 13.4 FR-POL-004 AI 智能匹配对话框

- 由 `openAiMatchDialog` 渲染（宽 720px，max-height 80vh）。触发条件：已同步未匹配的新政策数 `pending = _syncAddedCount - _aiMatchedCount`；若 `pending<=0` 弹确认框提示"请先点击「同步政策」"。
- **5 步流式思考日志**（逐步追加）：① 解析新政策文本（提取类型/范围/条件/力度）；② 扫描企业全生命周期画像库（N 家企业九维风险/经营/行业标签）；③ 多维度匹配评分（权重：行业契合度40%·企业规模20%·信用等级15%·营收能力15%·研发投入10%）；④ 交叉验证与去重（排除已到期/已享受同类、过滤高风险）；⑤ 生成匹配报告（每条新政策输出 Top N 及理由）。
- **匹配结果表**（每条新政策一张）：列＝ `#`（序号）/ 企业名称（含行业·规模副行）/ 匹配度（分，≥90 绿/≥80 蓝/≥70 橙/否则灰）/ 匹配依据（≤3 条理由）。
- **匹配概览**：本次处理新政策数、匹配企业/次总数、平均匹配度、置信度 92%、模型版本 `deepseek-v4-pro`。
- **确认匹配结果**："确认匹配结果"按钮将匹配企业写入 `e.policies`（保留最多 5 条）并更新政策 `entCount`（以 AI 结果为准）；底部"关闭"。

## 13.5 FR-POL-005 同步政策（候选池导入）

- 由 `openSyncDialog` 渲染（宽 460）：模拟从 7 个官方渠道（国家发改委/工信部/科技部/甘肃省人民政府/庆阳市政务服务网/庆阳市财政局/庆阳市招商局）逐个同步，进度条 + 状态勾选；完成后从 `POLICY_PENDING_POOL` 取 1–2 条新政策 `unshift` 至 `M.POLICY_LIB`（新 `code` 顺序编号），并按政策 `tag` 回写匹配企业的 `policies`（保留最多 5 条），递增 `_syncAddedCount`；3.5 秒后自动关闭并刷新列表，提示"同步完成，新增 N 条政策"。若已无可同步项提示"当前已是最新政策"。

## 13.6 全局操作

- **导出**：导出当前筛选下全部政策 CSV（`政策清单`），8 列＝编号 / 政策名称 / 层级 / 类型 / 发布部门 / 匹配企业数 / 年度安排(万元) / 已兑现(万元)。

---

# 十四、招商智能体需求（aidemo）

**模块定位**：嵌入业务流的 AI 智能服务入口，原型为四类智能体的对话式界面（预脚本确定性回答，非真实大模型调用）。

## 14.1 FR-AI-001 智能体矩阵（左侧）

- 四个智能体（`agents` 数组），每个含 `key` / `name` / 白色线性 SVG 图标 / 专属渐变底色 / `desc` 描述：
  1. **企业洞察智能体**（`insight`，青蓝渐变）：基于多源数据深度分析企业经营状态、发展潜力与风险特征，生成综合研判报告。
  2. **风险研判智能体**（`risk`，橙红渐变）：主动扫描企业经营异常信号，识别潜在风险并给出处置建议与影响评估。
  3. **招商谋划智能体**（`plan`，紫靛渐变）：基于产业链分析与区域定位，智能推荐招商方向、目标企业与落地路径。
  4. **企业服务智能体**（`service`，绿青渐变）：7×24 小时政策咨询、办事指引、诉求响应，提供一站式精准服务。
- 每个智能体有**独立对话历史**（`conversations[key]`，含预设开场白），切换智能体即从对应历史渲染；左侧卡片点击切换 `currentAgent` 并重渲染。

## 14.2 FR-AI-002 对话交互（右侧）

- **对话头部**：智能体图标 + 名称 + "在线·响应中"状态点 + 右侧"清空对话"按钮（重置为开场白）。
- **消息区**（`#aiMessages`）：用户/智能体双角色气泡，含头像、名称、时间（`HH:MM`）；智能体"思考中"显示三点动画，文本以打字机流式输出（按随机块长推进，`generating` 锁防止重复发送）。
- **快捷问题**（按智能体预设 `getQuickQuestions`）：点击填入输入框并自动发送，如 insight「分析一下 XX 能源」「营收前5」「新能源趋势」；risk「重大风险企业」「分析 XX 风险点」「近30天新增」；plan「重点招商方向」「新能源目标企业」「数字经济」；service「高企认定」「稳岗补贴」「诉求提交」。
- **输入框**：textarea，回车发送、`Shift+Enter` 换行；发送时推入用户消息 + 思考中占位，约 0.9–1.8 秒后由 `generateReply` 生成回答并流式输出；异常时兜底回复且不卡死发送状态。

## 14.3 FR-AI-003 预脚本确定性回答（generateReply）

- 按 `agentKey` + 问题关键词匹配确定性话术（含真实数据引用）：
  - **insight**：含企业名 → 输出「企业洞察报告」（基本概况/经营业绩/风险评估/综合评价，引用 `found/overview.regCapital/industryName/employees/overview.revenue/tax/invest/riskScore/LEVELS` 等）；问"前5/营收" → 营收 TOP5 列表（按 `revenueWan` 排序取 5）；问"新能源/趋势" → 全市新能源产业分析（企业数/营收取自 `INDUSTRIES` `newEnergy`/`neequip`）。
  - **risk**：问"重大风险/哪些" → 红色企业清单（`riskLevel==='red'`，含风险指数与主要风险维度）；含企业名 → 「风险研判报告」（**九维风险分析**话术口径 + 主要风险点 + 处置建议 4 条）；问"新增/近30天" → 近 30 天事件统计（`daysAgo<=30`，按等级与类型分布）。
  - **plan**：问"招商方向/重点" → 庆阳市 6 大重点招商方向（新能源及装备制造/石油化工及新材料/数字经济/中医药及健康养老/现代农业及食品加工/文化旅游）；问"新能源/目标企业" → 产业链目标企业推荐（整机/储能/氢能/本地配套 + 招商建议）；问"数字经济" → 算力/软件/AI/大数据招商路径与目标企业。
  - **service**：如"高新技术/高企" → 高新技术企业认定指南（注册年限/知识产权等条件）；"稳岗补贴""诉求提交"等均有对应话术。
- 注：风险智能体话术以"九维风险分析"表述，属**智能体话术口径**；demo 实际启用风险维度为 8 个（见第九章），二者不冲突，文档以第九章为准。

## 14.4 设计原则

- AI 不作为割裂独立平台，而是嵌入企业、项目、风险、服务、产业与决策流程；本原型以"招商智能体"集中入口承载四类能力，并在企业画像、政策服务等页面以按钮形式嵌入 AI 动作（AI 风险评估、AI 综合研判、AI 智能匹配）。

---

# 十五、数据体系

## 15.1 数据来源与生成约束

- demo 数据全部来自前端内置演示数据集 `demo/assets/data/mock.js`，以 **IIFE** 形式构建并挂载到 `window.MOCK`（`global.MOCK`）；**无任何外部数据接入、无后端、无网络请求**。
- **固定种子伪随机**：种子常量 `SEED = 20260822`，经 `mulberry32` 生成确定性序列，保证每次加载数据完全一致、可复现。
- 数据构建顺序：基础元数据（区县/行业/规模/风险维度/等级）→ 企业实体 → 风险事件 → 任务（与风险事件联动）→ 政策库 → 项目 → 派生聚合（`deriveAll`）→ 图谱（`buildGraph`）→ 日报/缺口/对标 → 导出对象。
- 历史版本描述的政府公共数据、企业数据、公开辅助数据接入为规划能力（见第十八章）。

## 15.2 核心导出对象清单（window.MOCK）

下列对象均通过 `global.MOCK = {...}` 导出，页面与测试脚本统一从 `M.<KEY>` 读取：

| 导出键 | 类型 | 规模/说明 |
| --- | --- | --- |
| `MONTHS` / `YEARS_5` / `MONTHS_6` | 数组 | 月份/近5年/近6月标签，供图表坐标轴 |
| `RISK_DIMS` | 数组(8) | 风险维度定义（见 15.4） |
| `LEVELS` | 对象 | 4 级风险（red/orange/yellow/blue）的名称/颜色/底色 |
| `DISTRICTS` | 数组(8) | 庆阳 8 区县 key/name |
| `ENTERPRISES` | 数组(120) | 企业主实体（见 15.3.1） |
| `DISTRICT_DATA` | 对象(8) | 每区县企业数、招商指标（派生） |
| `GEO_QINGYANG` | GeoJSON | 庆阳 8 区县行政区划矢量边界（驾驶舱热力图） |
| `DATA_SOURCES` | 数组(11) | 11 类数据来源（名称/企业数/更新频度/色点） |
| `INDUSTRIES` | 数组(12) | 12 行业聚合（营收等，派生） |
| `INDUSTRIES_META` | 数组(12) | 行业 key/name/color 元数据 |
| `OVERVIEW` | 对象 | 全市总览聚合（见 15.4） |
| `riskStats` | 函数 | 返回 4 级风险企业计数 |
| `RISK_EVENTS` | 数组(67) | 风险事件（见 15.3.2） |
| `PROJECT_STAGES` | 数组(6) | 6 阶段定义（key/name/order） |
| `PROJECTS` | 数组(19) | 招商项目（见 15.3.4） |
| `PROSPECT_ENTERPRISES` | 数组 | 招商目标（在谈）企业（谋划用） |
| `TASKS` | 数组(78) | 任务（见 15.3.3） |
| `AI_DAILY` | 对象 | 今日 AI 综述/行动项（驾驶舱） |
| `GRAPH` | 对象 | 图谱节点+边（见 15.3.5） |
| `INDUSTRY_GAP` | 数组(5) | 5 条产业链缺口（chain/gap/target/level） |
| `AI_CAPS` | 对象 | 4 项 AI 能力话术（看企业/找风险/做招商/帮服务） |
| `DEMO_SCRIPT` | 数组 | 一键演示剧本（页面/高亮/描述/时长） |
| `POLICY_LIB` | 数组(24) | 政策主实体（见 15.3.4） |
| `POLICY_PENDING_POOL` | 数组 | 待同步政策候选池（同步政策用） |
| `INVEST_TARGETS` | 对象 | 年度招商目标（总 180 亿 + 分区县） |
| `INVEST_STATS` | 对象 | 项目口径汇总（见 15.4） |
| `PROVINCE_COMPARE` | 数组(14) | 全省 14 市州到位资金对标（亿元） |
| `POLICY_REDEEM` | 对象 | 政策兑现统计（见 15.4） |
| `calcRiskScore` / `scoreToLevel` | 函数 | 风险加权分 / 分数→等级 |
| `entById` / `industryName` | 函数 | 企业查表 / 行业名查表 |

## 15.3 核心实体结构与字段

### 15.3.1 企业 ENTERPRISES（单条 `e`）

| 字段 | 类型 | 说明/取值 |
| --- | --- | --- |
| `id` | 字符串 | 企业主键（E01…） |
| `name` | 字符串 | 企业全称 |
| `creditCode` | 字符串 | 统一社会信用代码 |
| `legal` | 字符串 | 法定代表人 |
| `regCapital` / `regCapitalFmt` | 数值/字符串 | 注册资本（万元）/ 格式化（≥1亿→亿元） |
| `found` | 字符串 | 成立日期（`fmtDate`） |
| `industry` / `industryName` | 字符串 | 行业 key（12 类之一）/ 行业中文名 |
| `scale` | 字符串 | 大型/中型/小型/微型企业 |
| `tags` | 数组 | 企业标签（龙头/高新/专精特新等） |
| `district` / `districtName` | 字符串 | 区县 key / 中文名 |
| `address` | 字符串 | 注册地址 |
| `isDeep` | 布尔 | 是否深度企业（生成 `ai` 研判） |
| `overview` | 对象 | `regCapital`/`revenue`(格式化)/`revenueWan`/`tax`(万元)/`taxWan`/`employees`/`invest`(格式化)/`investWan`/`profit`(万元)/`yearly`(近5年 revenue/tax/employees/investWan 数组) |
| `status` | 对象 | `biz`(经营状态)/`credit`(信用状态)/`performRate`(履约率%) |
| `operation` | 对象 | 近 6 期 `revenue`(亿)/`tax`/`invest`/`employees` 数组 |
| `commitments` | 数组 | 招商承诺（name/promise/actual/unit），如营收、纳税、就业岗位 |
| `dynamics` | 数组 | 企业动态（date/type/text） |
| `risks` | 对象 | 8 维度风险分（key=value，0–100） |
| `riskScore` / `riskLevel` | 数值/字符串 | 加权综合分（`calcRiskScore`）/ 等级（`scoreToLevel`） |
| `shareholders` | 数组 | 股东（name/ratio%） |
| `policies` | 数组 | 匹配政策名（≤5，由政策库回写） |
| `ai` | 对象/null | 深度企业 AI 研判（summary/strengths/risks/sources/confidence/time/prevScore/lastJudge），非深度为 null |
| `landMu` | 数值 | 用地面积（亩，由 `investWan/90` 推导，≥15） |

### 15.3.2 风险事件 RISK_EVENTS（单条 `ev`）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | 字符串 | 事件 ID（R01…） |
| `time` / `timeHm` | 字符串 | 发现日期 / 时分 |
| `entId` / `enterprise` / `enterpriseName` | 字符串 | 涉事企业 ID / 名称 |
| `title` / `finding` | 字符串 | 事件标题 / 发现摘要 |
| `dim` / `dimKey` / `dimName` | 字符串 | 风险维度 key / 中文名 |
| `level` | 字符串 | red/orange/yellow/blue |
| `status` | 字符串 | 待处置 / 已派发 / 已关闭 |
| `detail` / `basis` / `advice` / `suggestion` | 字符串 | 详情 / 依据 / 处置建议 |
| `daysAgo` | 数值 | 距今天数（用于近 30 天统计） |
| `type` / `typeKey` | 字符串 | 维度异常类型 |

### 15.3.3 任务 TASKS（单条 `t`）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | 字符串 | 任务 ID（T01… / TS…） |
| `title` | 字符串 | 任务标题（风险处置类前缀"【风险处置】"） |
| `enterprise` / `enterpriseName` | 字符串 | 关联企业 ID / 名称 |
| `type` | 字符串 | 风险处置/项目跟进/企业服务/政策推送/日常巡检（与工作台筛选项对齐） |
| `priority` | 字符串 | 高/中/低 |
| `status` | 字符串 | 待处理/进行中/已逾期/已完成 |
| `due` / `createTime` / `completeTime` | 字符串 | 截止日 / 创建日 / 完成日（已完成才有） |
| `source` | 字符串 | 来源（风险事件 ID / 政策 code / 空） |

生成逻辑：风险处置任务与 `RISK_EVENTS` 严格一一对应（见 15.5）；另含 10 条常规招商任务 + 一批补充任务（覆盖全部 5 种类型、含数条"今日到期"供今日待办展示），纯确定性构造、不消耗随机数。

### 15.3.4 政策 POLICY_LIB（单条 `p`）/ 项目 PROJECTS（单条 `pj`）

- **政策** `p`：`code`(P01…)/`name`/`dept`/`type`/`level`/`apply`(适用)/`brief`(摘要)/`support`(支持方式)/`materials`(申报材料)/`date`/`tag`(匹配标签)/`entCount`(匹配企业数)/`planWan`(年度安排万)/`redeemedWan`(已兑现万)/`redeemRate`(兑现率%)——后四项由 `POLICY_REDEEM` 推导写入。
- **项目** `pj`：`id`/`name`/`ent`(关联企业)/`stage`(6 阶段之一)/`district`/`unit`(责任单位)/`amountWan`(总投资万)/`progress`(进度%)/`risk`(风险等级) + 关键节点时间轴。

### 15.3.5 图谱 GRAPH（节点 + 边）

- 由 `buildGraph()` 生成，输出 `{nodes, links}`（**46 节点 + 117 边**：15 家企业 + 6 投资平台 + 8 政府部门 + 10 政策 + 4 招商线索 + 3 外部市场），节点类别：企业/投资平台/政府部门/匹配政策/招商线索/外部市场；边含 `source/target/type/relation`（8 类业务语义，见 10.1.4）。
- 节点字段：`id`/`name`/`category`/`district`/`industry`/`scale`/`riskLevel`/`landGap`/`fundGap` 等；边字段：`source`/`target`/`type`(业务语义)/`relation`(关系名)。

## 15.4 派生量与计算口径

- **加权风险综合分** `calcRiskScore(risks)`：对 `RISK_DIMS` 中 `weight>0` 的维度，`s += weight × risks[key]`，四舍五入；权重合计 = 1（0.2+0.15×3+0.1×3+0.05）。
- **分数→等级** `scoreToLevel(score)`：`≥65→red`(重大) / `≥45→orange`(较高) / `≥25→yellow`(一般) / `<25→blue`(关注)。
- **风险维度** `RISK_DIMS`（8 启用，key→name→weight）：operation 经营 0.2、finance 财务 0.15、judicial 司法 0.15、credit 信用 0.15、tender 招投标 0.1、tax 税务 0.1、perform 招商履约 0.1、ip 知识产权 0.05。
- **招商项目口径** `INVEST_STATS`：签约落地（`projStageOrder≥3`）起计入"已签约"；**到位资金 = 协议额(万) × 进度%**（四舍五入）；签约前计入"在谈储备"(`pipelineWan`)；含 `stageCounts`(各阶段计数) 与 `byDistrict`(分区县 agreedWan/arrivedWan/count)。
- **年度招商目标** `INVEST_TARGETS`：年度总额 180 亿（`year=YEARS_5[4]`）+ 分 8 区县 `byDistrict`（xf55/qc32/hj8/hn16/hy11/zq18/ning9/zx31，单位亿）。
- **全省对标** `PROVINCE_COMPARE`：以庆阳推导到位资金(亿)为基准，按倍数构造 14 市州（兰州市 4.2×、天水 1.18×…），排序后保证庆阳"列全省第 3 位"稳定。
- **政策兑现** `POLICY_REDEEM`：逐政策按序号确定性推导 `planWan=((idx*97+61)%19+6)*100`（600–2400 万）、`rate=((idx*37+53)%40+55)/100`（55%–94%），`redeemedWan=plan×rate`；`entCount` = 企业 `policies` 命中该政策名数；汇总 `planWan/redeemedWan/rate/entsHelped`(去重企业数)。
- **全市总览** `OVERVIEW`：企业总数/重点/本月新增、总营收/纳税/投资(亿，由万/10000)、带动就业、风险企业数(红+橙+黄)、`mom`(环比，伪随机 1 位小数)、`updateTime`。
- **企业用地** `e.landMu = max(15, round(investWan/90))`（约 90 万元/亩），供亩均税收口径使用。
- **产业链缺口** `INDUSTRY_GAP`：5 条（算力/数字经济、新能源装备、生物医药、石油化工、现代农业），每条含 `gap`(缺口)/`target`(招商目标)/`level`(高/中/低)。

## 15.5 数据间引用关系

- **企业 ↔ 风险事件**：`RISK_EVENTS.entId` 外键指向 `ENTERPRISES.id`；风险页/企业画像据此反查。
- **风险事件 ↔ 任务（闭环）**：`RISK_EVENTS.status` 与 `TASKS`(`type:'风险处置'`) 一一对应——已派发事件→未完成任务（待处理/进行中/已逾期），已关闭事件→已完成任务（完成日晚于发现日），待处置事件不生成任务（`source=ev.id` 关联）。
- **企业 ↔ 政策**：`ENTERPRISES.policies`(≤5) 与 `POLICY_LIB.name` 互引；`POLICY_REDEEM.entCount` 由企业 `policies` 命中计数；"同步政策/AI 智能匹配"会回写企业 `policies` 与政策 `entCount`。
- **企业 ↔ 项目**：`PROJECTS.ent` 关联企业；驾驶舱/项目页据此串联。
- **企业 ↔ 图谱**：`GRAPH.nodes` 含企业（及平台/政府/政策/线索/市场）节点，`links` 表达股权/供应链/市场/政策等关系，与 `ENTERPRISES` 字段（行业/区县/规模/风险）一致。
- **项目 ↔ 招商目标**：`INVEST_TARGETS`/`INVEST_STATS` 由 `PROJECTS` 聚合；`PROSPECT_ENTERPRISES` 为谋划用目标企业。
- **企业 → AI 研判**：深度企业 `e.ai` 由 `buildJudgeResult`/画像页生成；`AI_DAILY` 综述来自当前辖区聚合。

## 15.6 一致性与可重算

- **一致性校验**：企业总数 = 区县企业数合计；风险分布合计 = 企业总数；行业营收合计 ≈ 企业营收合计（`tools/smoke_interaction.js` 数据对账）；风险事件三态 × 任务数量守恒、时序自洽（`tools/check_invariants.js`）。
- **可重算引擎** `MOCK_ENGINE`：`enrich`（企业政策匹配/政策日期/AI 研判）+ `deriveAll`（聚合、图谱、日报）+ `rebuild(raw)`：以服务器实体数据（ENTERPRISES/RISK_EVENTS/PROJECTS/TASKS/POLICY_LIB）重建全部派生结构；重建使用固定种子随机，保证同一份数据多次重建结果完全一致。该引擎供前后端分离版（`web/` + `server/`）在真实数据上复用 demo 的聚合/图谱逻辑。

---

# 十六、导出与报告能力

## 16.1 CSV 表格导出

- 驾驶舱、企业概况、风险预警、我的工作台、政策服务均支持将当前视图导出为 CSV（`components.js` 的 `exportCSV`，文件名带日期）。

## 16.2 企业画像 PDF 报告（文字版）

- 企业画像"导出报告"按钮生成 **A4 文字版 PDF 报告**（十三章结构，与打印版同源同构）。
- 技术链路：`jsPDF` + `jspdf.plugin.autotable`（表格） + 按需懒加载的中文子集 `pdf-font-zh.js`，输出**矢量文字、可选中、可检索、体积小**。
- 字体子集由 `tools/make_pdf_font.py` 生成，置于 `assets/vendor/`。

## 16.3 图谱图片导出

- 关系图谱支持导出当前视图为图片。

---

# 十七、技术实现要求

## 17.1 前端技术栈

- 原生 JavaScript（无框架构建步骤）+ ECharts 图表 + jsPDF/autoTable 导出。
- 单页应用，页面渲染器注册表（`APP.registerRenderer`）+ 全局状态（`state.js`）。
- 纯静态资源，离线可用，可直接 `python -m http.server` 托管。

## 17.2 统一组件与状态

- 公共组件：`components.js` 提供 confirm / toast / 抽屉 / 弹窗 / 表格等。
- 状态处理：全局 `state` 管理页面与筛选条件；页面切换保持业务上下文（如企业对象、当前页签、查询条件）。
- 交互一致性：信息与动作相邻（看到异常即可查看详情/AI 分析/创建任务/派发/生成报告）。

## 17.3 响应式

- 页面适配窄屏：驾驶舱产业环图移动端将图例移至饼图下方、横向自动换行完整显示（扇区不叠加标签，不截断不挤压）；企业画像信用代码、经营趋势、AI 综合研判三块在窄屏自适应不挤压；关系图谱、政策等同步适配。

---

# 十八、非功能与验收基线

## 18.1 可用性

- 浏览器直接打开即可运行，无需安装与后端。
- 9 个模块均可进入并渲染；登录门禁、全局搜索、一键演示、全屏、注销均可用。

## 18.2 验收方法

- 功能验收以本文"第六章至第十四章"的 9 个模块逐项核对 demo 实现为准。
- 自动化冒烟：`tools/smoke_test.js`（9 页全部渲染）、`tools/smoke_interaction.js`（数据对账 + 下钻链路）、`tools/verify_login.cjs`（登录门禁）、`tools/verify_industry_responsive.cjs` / `verify_profile_responsive.cjs` / `verify_policy_jump.cjs`（移动端与跳转修复校验）。

## 18.3 规划能力说明（不在本次原型设计范围）

- 真实后端服务与数据库落库；
- 真实鉴权与多角色权限（RBAC）、操作审计；
- 真实 AI 模型调用与证据链；
- 企业侧服务门户与诉求办理闭环；
- 数据治理、数据标准与质量监控。

上述能力在 `server/` + `web/` 前后端分离版中有部分真实落地（真实 JWT 登录、真实接口、操作落库），可作为 demo 原型向工程化演进的参考，但**不计入本说明书的功能验收基线**。

---

# 十九、总结

本深化版说明书已**严格收敛并对齐 `demo` 高保真原型的真实实现**：系统架构描述纯前端单页应用的分层与技术依赖；功能菜单精确对应 demo 的 4 组 9 模块导航；功能点逐项描述 9 个模块的实际页面、交互、数据与导出能力。凡是 demo 尚未实现的后端、真实鉴权、真实 AI 与多角色权限等，一律明确标注为规划能力，确保需求说明书与 demo 实现严格一致。
